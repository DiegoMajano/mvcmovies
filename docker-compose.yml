version: "3.9"

services:
  postgres:
    image: postgres:16-alpine
    container_name: postgres_peliculas
    environment:
      POSTGRES_DB: "Peliculas"
      POSTGRES_USER: "postgres"
      POSTGRES_PASSWORD: "TuPassword123!" 
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d Peliculas"]
      interval: 5s
      timeout: 5s
      retries: 10

  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: mvcpeliculas_web
    depends_on:
      postgres:
        condition: service_healthy
    ports:
      - "8080:8080"
    environment:
      ASPNETCORE_ENVIRONMENT: "Development"
      ASPNETCORE_URLS: "http://+:8080"
      ConnectionStrings__DefaultConnection: "Host=postgres;Port=5432;Database=Peliculas;Username=postgres;Password=TuPassword123!"

volumes:
  postgres_data:
